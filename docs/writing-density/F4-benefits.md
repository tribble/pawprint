[Context: you are writing the "Why" section of a design doc, spikes/api-services-barrel-design.md, for engineers and agents in the workos monorepo. The proposal replaces the single root barrel of @workos-inc/api-services with per-directory entry points via a package.json exports map. Measured facts: on one scoped api test boot, 2,037 api-services files were evaluated; with owning-file imports instead, 1,161 — about 880 files were loaded only because the barrel listed them. Every pod, worker and job runner requires the same barrel at start (their individual numbers were not measured). There is no bundler in this repo, and Node caches required modules, so bundle size and warm-process speed are unaffected. Fewer files evaluated means fewer import cycles are reachable; real cycles (ActionsEndpointsService <-> ActionsExecutionsService) remain and keep their forwardRef. The reader has already decided to read the doc; write the bullets they need. Output only the bullets.]

Write the "Why" section: what this change buys us.
