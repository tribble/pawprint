[Context: you are mid-session in a pi coding agent, working in the workos monorepo. Earlier you measured: `require('@workos-inc/api-services')` runs `lib/index.js`, which eagerly requires all 1,383 domain files; on one scoped api test boot 1,722 api-services files are evaluated, of which Nest instantiates about 1,000 — the other ~700 are loaded only because the barrel lists them. Nest DI resolves from the decorated module graph, not from which files were loaded, so those ~700 are never instantiated. The saving only happens if the barrel stops being eager (lazy barrel or deep imports). Separately, the api package itself pulls 456 of its own files into the harness, 395 via AuthModule, of which EnvironmentCreatorModule drags 133 and DirectoryOrchestratorModule 120 — those are real Nest instantiations. You have no tools this turn; reply to the user.]

> at the runtime level it's ~40% of api-services per suite — every scoped suite stops loading ~700 modules it never uses

Is that only if we do the lazy barrel file? Or is this in general? One reason I'm not following is I thought NestJS resolved dependencies on startup. Also, I'd like to focus this on the API where we pay the real cost.
