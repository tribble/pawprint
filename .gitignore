# pawprint — default-deny: nothing is tracked unless allowlisted below.
# ~/.pi is a sparse worktree of this repo (cone: agent/), so this file is also
# what keeps auth.json, sessions/, git/ and every other runtime file pi writes
# under agent/ invisible to git. Allowlist directories, never `agent/**`;
# agent/bin/ is pi's (fd, rg) and stays ignored: a binary dropped there must
# never become trackable.
*
!.gitignore
!.githooks/
!.githooks/pre-commit
!.gitleaksignore
!README.md
!AGENTS.md
!setup.sh
!scripts/
!scripts/validate.sh
!scripts/sync-forks.sh
!scripts/patch-pi-anthropic-gateway
!manifest.json
!biome.json
!package.json
!tsconfig.json
!mise.toml
!ghostty/
!ghostty/config.ghostty
!extensions/
!extensions/**
!prompts/
!prompts/**
!skills/
!skills/**
!themes/
!themes/**
!tests/
!tests/**
!agent/
!agent/AGENTS.md
!agent/cloak.json
!agent/fitch-mcp-adapter/
!agent/fitch-mcp-adapter/mcp.json
!agent/models.json
!agent/presets.json
!agent/settings.json
!agent/agents/
!agent/agents/**
!agent/configs/
!agent/configs/**
*.bak-pawprint-*
